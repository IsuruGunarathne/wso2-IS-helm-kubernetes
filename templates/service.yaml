apiVersion: v1
kind: Service
metadata:
  name: {{ include "wso2-IS-helm-kubernetes.fullname" . }}
  labels:
    {{- include "wso2-IS-helm-kubernetes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wso2-IS-helm-kubernetes.selectorLabels" . | nindent 4 }}
