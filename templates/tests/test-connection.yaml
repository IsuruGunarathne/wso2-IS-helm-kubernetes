apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wso2-IS-helm-kubernetes.fullname" . }}-test-connection"
  labels:
    {{- include "wso2-IS-helm-kubernetes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wso2-IS-helm-kubernetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
